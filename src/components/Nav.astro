---
import Icon from "./Icon.astro";

/** Test if a link is pointing to the current page. */
const isCurrentPage = (href: string) => {
	let pathname = Astro.url.pathname.replace(import.meta.env.BASE_URL, "");
	if (pathname.at(0) !== "/") pathname = "/" + pathname;
	if (pathname.at(-1) !== "/") pathname += "/";
	return pathname === href || (href !== "/" && pathname.startsWith(href));
};
---

<nav>
	<div class="menu-header">
		<a href="/" class="nav_link">
			<Icon
				icon="house"
				color="var(--accent-regular)"
				size="1.6em"
				gradient
			/>
			Home
		</a>
		<a href="https://github.com/GizmoAhmed" class="nav_link">
			<Icon
				icon="github-logo"
				color="var(--accent-regular)"
				size="1.6em"
				gradient
			/>
			GitHub
		</a>
		<a href="https://www.linkedin.com/in/mohamed-ahmed-460a2a240/" class="nav_link">
			<Icon
				icon="linkedin-logo"
				color="var(--accent-regular)"
				size="1.6em"
				gradient
			/>
			LinkedIn
		</a>
		<a href="https://gizmo435.itch.io/" class="nav_link">
			<Icon
				icon="game-controller"
				color="var(--accent-regular)"
				size="1.6em"
				gradient
			/>
			Itch.io
		</a>
		<a href="https://mail.google.com/mail/u/0/#inbox?compose=GTvVlcSHxjWGhxVmvnpQQGmRNwXqjbccRjnLSDknQtvlBgLPnmznrvpHfMTgCbnwSsDBpzQPqsZsx" class="nav_link">
			<Icon
				icon="paper-plane-tilt"
				color="var(--accent-regular)"
				size="1.6em"
				gradient
			/>
			Contact Me
		</a>
</nav>

<style>
	.menu-header {
		display: flex;
		justify-content: center;
		gap: 13rem;
		padding: 3.5rem;
	}

	.nav_link {
		display: flex;
		gap: 0.7rem;
		align-items: center;
		line-height: 1.1;
		color: var(--gray-0);
		text-decoration: none;
	}

	@media (min-width: 50em) {
		nav {
			display: row;
			grid-template-columns: 1fr auto 1fr;
			align-items: center;
			padding: 2.5rem 5rem;
			gap: 1rem;
		}

		.menu-header {
			padding: 0;
		}
	}

</style>
